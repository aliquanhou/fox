# P0-11.2.1 Real Call Graph Evidence Repair

**阶段**: P0-11.2.1
**目标**: 修正 Call Graph Evidence 标签，明确说明是基于 Object Access
**基线**: `7efd1e7` (P0-11.2 Audit)
**日期**: 2026-09-12

---

## 1. 修改文件

| 文件 | 修改内容 |
|------|----------|
| `crates/fox-decompiler/src/emitter.rs` | 修正 Call Graph Evidence 标签为 Object Access Graph |

**未修改**: SSA、Memory SSA、Expression Recovery、CFG、ConditionRecovery、CallGraph、DynamicPluginResolver、Structured IR 等所有已封板模块。

---

## 2. 修正内容

### 2.1 输出标签修正

```
*   P0-11.2.1 Object Access Graph: N objects, M fields, K accesses (call graph: GAP)
```

### 2.2 Role 标签修正

| 旧标签 | 新标签 |
|--------|--------|
| hub-candidate | hub-candidate (access-based) |
| leaf-candidate | leaf-candidate (access-based) |

### 2.3 Evidence 修正

明确说明：
```
accesses=X, fields=Y (NOT call count)
```

---

## 3. NtcMach 真实 Dogfood

| 指标 | 数值 |
|------|------|
| 339 OK | ✅ 保持 |
| 函数证据输出 | 97 个函数输出 |

---

## 4. 已知限制（GAP）

### GAP-P0-11.2.1-REAL-CALL-GRAPH（P1）
当前 Graph Role 基于 global access count，不是 call graph edges。
真正的 Call Graph Evidence 需要从 CallStmt 收集 caller/callee 关系。

---

## 5. Tests

| 测试套件 | 结果 |
|----------|------|
| fox-decompiler lib tests | 42 PASS |
| 全部 integration tests | 27 PASS |
| **总计** | **69 PASS, 0 FAIL** |

- `cargo fmt`: PASS
- `cargo clippy --all-targets -- -D warnings`: PASS

---

## 6. 结论

P0-11.2.1 修正了 Call Graph Evidence 标签，明确说明是基于 Object Access，不是真正的 Call Graph。

**状态**: IMPLEMENTATION COMPLETE → WAITING FOR AUDIT
