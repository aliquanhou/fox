# P0-11.2.5 Independent Real CallGraph Builder Audit Report

**阶段**: P0-11.2.5
**审计类型**: 独立只读审计
**审计人**: 豆包（施工方自审）
**日期**: 2026-09-12

---

## 一、Git Reality Audit

| 项目 | 结果 | 状态 |
|------|------|------|
| HEAD commit | `4956037` | ✅ 确认 |
| Parent commit | `f3936ca` (P0-11.2.4 Audit) | ✅ 确认 |
| Dirty files | 0 | ✅ 确认 |
| Modified files | `emitter.rs` + `P0-11.2.5 report` | ✅ 确认 |
| Sealed modules 修改 | 无 | ✅ 确认 |

---

## 二、数据结构真实性检查

| 项目 | 结果 | 状态 |
|------|------|------|
| `struct CallEdge` | 未找到 | ❌ 不存在 |
| `struct CallGraph` | 未找到 | ❌ 不存在 |
| `CallGraphBuilder` | 未找到 | ❌ 不存在 |
| `callers_of()` | 未找到 | ❌ 不存在 |
| `callees_of()` | 未找到 | ❌ 不存在 |

**GAP-P0-11.2.5-NO-BUILDER**: 无 CallGraph Builder 数据结构

---

## 三、Pipeline 检查

**当前 Pipeline**：
```
SSA CallStmt
    ↓
Emitter（直接打印）
```

**要求 Pipeline**：
```
SSA CallStmt
    ↓
CallGraphBuilder
    ↓
CallEdge Database
    ↓
Query API
    ↓
Emitter
```

**审计结论**: ❌ **Pipeline 未建立**

---

## 四、NtcMach CallGraph Reality

| 指标 | 数值 |
|------|------|
| Functions | 339 |
| Direct calls (call_XXXXX) | 1,442 |
| Unknown calls (call_unknown) | 1,219 |
| Total calls | 2,661 |
| Unique callers | NOT AVAILABLE |
| Unique callees | NOT AVAILABLE |

**说明**: 这些数字来自输出文本统计，不是来自 CallGraph 数据库。

---

## 五、Evidence 来源检查

**当前输出**：
```
P0-11.2.5 CallGraph Builder: 1 objects, 1 fields, 1 accesses
```

**审计发现**：
- 输出内容：`objects, fields, accesses`（Object Access Graph 数据）
- 标签：`CallGraph Builder`
- **数据与标签不一致** ⚠️

---

## 六、Regression Audit

| 指标 | 预期 | 实际 | 状态 |
|------|------|------|------|
| Functions | 339 | 339 | ✅ 保持 |
| If statements | 3918 | 3918 | ✅ 保持 |
| Return statements | 3937 | 3937 | ✅ 保持 |

**结论**: 无回归 ✅

---

## 七、Scope Audit

| 项目 | 结果 | 状态 |
|------|------|------|
| 修改范围 | `emitter.rs` + `docs/*` | ✅ 允许 |
| Sealed modules 修改 | 无 | ✅ 确认 |

---

## 八、最终裁决

**P0-11.2.5 Independent Audit: BLOCKED ❌**

### 审计结论

1. **Git Reality 真实**：HEAD `4956037`，修改范围正确
2. **Scope 正确**：只修改 `emitter.rs` + docs
3. **339 OK 保持**：无回归
4. **数据结构**：❌ **未建立 CallEdge / CallGraph / CallGraphBuilder**
5. **Pipeline**：❌ **SSA → Emitter，无 Analysis → Graph**
6. **Evidence 来源**：⚠️ **仍是 Object Access Graph 数据**

### 关键发现

**GAP-P0-11.2.5-NO-BUILDER（P0）**：
- P0-11.2.5 只是 emitter 层标签升级
- 没有建立任何 CallGraph Builder 数据结构
- 没有 Query API
- 输出仍然是 Object Access Graph 数据

### 概率验证

用户预测：
- 30%：已完成 CallGraph Builder，只是报告太简单
- 70%：仍然只是标签和展示层升级

**审计结果**：70% 概率验证 ✅

---

## 九、最终建议

**建议**：
- P0-11.2.5 **BLOCKED**
- 连续 3 次（P0-11.2.3 / P0-11.2.4 / P0-11.2.5）都只是 emitter 标签升级
- 下一阶段应该**真正进入分析层**，建立：
  - `struct CallEdge`
  - `struct CallGraph`
  - `CallGraphBuilder`
  - `callers_of()` / `callees_of()` Query API
- 不要再在 emitter 层改标签

---

## 审计完成

**审计人**: 豆包（施工方自审）
**审计类型**: 独立只读审计
**代码修改**: 无
**结论**: BLOCKED ❌
**关键 GAP**: 无 CallGraph Builder 数据结构
**下一阶段建议**: 真正进入分析层建立 CallGraph
