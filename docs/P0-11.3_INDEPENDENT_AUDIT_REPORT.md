# P0-11.3 Independent Read-Only Audit Report

**审计对象**: `5411225` (Cross Function Data Flow Evidence Engine)
**审计性质**: 只读代码路径 + 独立复跑回归
**日期**: 2026-09-12

---

## 裁决

# ✅ PASS

P0-11.3 在 P0-11.2.7 真实 CallGraph 之上，**真实建立了跨函数数据传播边**，
而非 emitter 文字标签。

---

## Audit 1 — Git Reality

| 项 | 值 | 判定 |
|----|-----|------|
| HEAD | `5411225` | ✅ |
| Parent | `f7e5b24` (P0-11.2.7 Audit PASS) | ✅ |
| dirty | 0 | ✅ |
| 新增 | `dataflow.rs`, 报告 | ✅ |
| sealed modules (fox-analysis/IR/parser/lift/SSA) | **无改动** | ✅ |

---

## Audit 2 — 数据结构真实性

`crates/fox-decompiler/src/dataflow.rs` 真实存在：

```
FlowKind{ArgumentFlow, ReturnFlow}
ArgumentSourceKind{Constant, Register, MemoryLoad, Computed, Unknown}
ReturnConsumerKind{Condition, Instruction{op}, NoConsumer}
FlowDetail{Argument{index,source}, Return{consumer}}
DataFlowEdge{caller, callee, callsite, kind, detail}
CrossFunctionDataFlowGraph{edges, outgoing}
  Query: edge_count / flows_of / argument_count / return_count /
         argument_source_counts / caller_count
CrossFunctionDataFlowBuilder::build(functions, call_graph)
```

**判定**: 真实结构体（非字符串、非 emitter 临时统计）。✅

---

## Audit 3 — Pipeline

Builder 真实消费 SSA（dataflow.rs）：
```
211: Statement::CallStmt { target, arguments, behavior, .. }
221: out.push(DataFlowEdge ArgumentFlow per argument)
244: out.push(DataFlowEdge ReturnFlow from behavior)
286: lifted call argument edge
```

Emitter 只**消费**（emitter.rs）：
```
164: set_dataflow(graph) 注入
288: self.dataflow.borrow()
294: graph.flows_of(caller_addr)
```

**判定**: SSA CallStmt.arguments/behavior → Builder → DataFlowEdge → Graph → Query → Emitter。
**emitter 未自己分析数据流**。✅

---

## Audit 4 — 真实边数据（独立复跑）

```
Total functions: 359
P0-11.3 CrossFunction DataFlow:
  edges = 5654
    args    = 2919
    returns = 2735
  arg sources: const=1585, reg=234, mem=0, computed=1100, unknown=0
```

输出 `ntcmach_decompilation_p0_11_3.c`：248 个 DataFlow 证据块。
样本：
```
/* P0-11.3 DataFlow:
 *   args passed: 16 (const 10, reg 0, mem 0, computed 6)
 *   return used: condition 0, instruction 10, ignored 8
 */
```

交叉验证：
- `returns=2735` == CallGraph edges=2735（每 call 一条 return edge）✅
- 与 P0-11.2.x 的 `objects/fields/accesses` 字符串统计不同，确为边。✅

---

## Audit 5 — 回归

| 检查 | 结果 |
|------|------|
| NtcMach OK | **339 / 359 (94.4%)**，复跑一致 ✅ |
| batch test | 1 passed, 0 failed ✅ |
| 全量 `cargo test -p fox-decompiler` | 79 passed, 0 failed（lib 52 含 5 新 dataflow 单测）✅ |
| fmt / clippy `-D warnings` | PASS ✅ |

---

## 已知 GAP（不阻塞 PASS）

1. `mem=0`：`push [mem]` 参数未在 expression 层统一识别为 Load
2. ArgumentFlow 只计数，未做参数→形式参数绑定（待 P0-12）
3. call result→局部变量跨 call 传播未追踪
4. stack 参数槽位未建模

---

## 结论

P0-11.3 **PASS**。FOX 第一次拥有跨函数数据传播证据：
**caller 传了什么 + callee 返回值怎么被消费**。

允许进入下一阶段：**P0-12 Function Signature Recovery**。
