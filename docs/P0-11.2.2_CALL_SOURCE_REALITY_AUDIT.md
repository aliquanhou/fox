# P0-11.2.2 Call Source Reality Audit

**阶段**: P0-11.2.2
**审计类型**: 只读审计
**审计人**: 豆包（施工方）
**日期**: 2026-09-12

---

## 一、基线确认

| 项目 | 结果 |
|------|------|
| HEAD commit | `d8707c9` (P0-11.2.1) |
| Parent commit | `7efd1e7` (P0-11.2 Audit) |
| Dirty files | 0 |
| Branch | main |
| 代码修改 | 无（只读审计） |

---

## 二、IR Call 能力检查

### 2.1 Expression::Call

**文件**: `crates/fox-decompiler/src/expression.rs`

```rust
pub enum Expression {
    Call {
        target: CallTarget,
        arguments: Vec<Expression>,
    },
    // ...
}
```

### 2.2 CallTarget 枚举

```rust
pub enum CallTarget {
    Address(u64),      // Direct address
    Symbol(String),    // Symbolic name (imported function)
    Unknown,           // Indirect / unresolved
}
```

**结论**: IR 层已支持 Call 结构 ✅

---

## 三、Lift 层检查

**文件**: `crates/fox-decompiler/src/structured_ir.rs`

### 3.1 CallStmt

```rust
CallStmt {
    target: CallTarget,
    arguments: Vec<Expression>,
    behavior: Option<CallBehavior>,
}
```

### 3.2 CallBehavior 枚举

```rust
pub enum CallBehavior {
    ReturnUsedInCondition { condition, consumer_instruction, branch_instruction },
    ReturnUsedByInstruction { consumer_instruction, consumer_op },
    NoConsumer,
}
```

**结论**: Lift 层已支持 CallStmt + CallBehavior ✅

---

## 四、SSA 层检查

| 能力 | 状态 |
|------|------|
| Direct Call target | ✅ YES (CallTarget::Address) |
| Indirect Call target | 🟡 PARTIAL (CallTarget::Unknown + dynamic_plugin.rs 解析 7 个) |
| Call arguments | 🟡 PARTIAL (CallStmt 有 arguments 字段，但未完全恢复) |
| Caller function id | ❌ NO (DecompilerFunction 有 address，但无 caller 列表) |
| Call site address | 🟡 PARTIAL (CallBehavior 有 consumer_instruction) |

---

## 五、Emitter 能力检查

| 能力 | 状态 |
|------|------|
| 访问 DecompilerFunction | ✅ YES |
| 遍历 CallStmt | ✅ YES |
| 提取 CallTarget | ✅ YES |
| 统计 caller/callee | ❌ NO (无全局 call graph 数据结构) |

---

## 六、NtcMach 样本统计

| 指标 | 数值 |
|------|------|
| Total functions | 339 |
| call_ references | 2,661 |
| call_unknown | 1,219 |
| NTCDLL references | 0 (动态调用已解析为其他形式) |

---

## 七、Call Edge 可实现性判断

```
CALL GRAPH READINESS

Direct Edge:     READY ✅
Indirect Edge:   PARTIAL 🟡 (7 个已解析)
Argument Flow:   PARTIAL 🟡
Caller/Callee:   NOT READY ❌ (无全局 call graph 数据结构)
```

---

## 八、GAP 清单

| GAP-ID | 描述 | 影响 | 解决阶段 |
|--------|------|------|----------|
| GAP-P0-11.2.2-001 | 无全局 CallGraph 数据结构 | 无法统计 caller/callee | P0-11.2.3 |
| GAP-P0-11.2.2-002 | Caller 列表未收集 | 无法检测 hub function | P0-11.2.3 |
| GAP-P0-11.2.2-003 | Indirect call target 未完全解析 | 部分 call edge 缺失 | P0-7.1B 后续 |
| GAP-P0-11.2.2-004 | Call arguments 未完全恢复 | 无法做参数数据流 | P0-11.3 |

---

## 九、与 Object Graph 区分

| 类型 | 当前状态 | 数据来源 |
|------|----------|----------|
| Object Access Graph | ✅ 已实现 | global field access count |
| Call Graph | ❌ 未实现 | 需要 CallStmt 全局遍历 |

**明确区分**:
- 当前 P0-11.2.1 输出是 **Object Access Graph**
- **不是** Call Graph

---

## 十、回归确认

| 指标 | 状态 |
|------|------|
| 339 OK | ✅ 保持 |
| 69 tests | ✅ PASS |
| 代码修改 | 无（只读审计） |

---

## 十一、最终结论

```
FOX Call Graph Readiness: FOUNDATION READY
```

**理由**:
1. IR 层已有 `Expression::Call` 和 `CallStmt` 结构 ✅
2. `CallTarget` 已区分 Direct/Indirect/Unknown ✅
3. `CallBehavior` 已恢复返回值消费者 ✅
4. **缺失**: 全局 CallGraph 数据结构（caller/callee 统计）❌

---

## 十二、下一阶段建议

**建议**: P0-11.2.3 Real Call Edge Extraction

**目标**:
1. 新增全局 CallGraph 数据结构
2. 遍历所有函数收集 CallStmt
3. 建立 caller → callee 边
4. 统计 caller_count / callee_count
5. 输出真正的 Call Graph Evidence

---

## 审计完成

**审计人**: 豆包（施工方）
**审计类型**: 只读审计
**代码修改**: 无
**结论**: FOUNDATION READY
**下一阶段**: P0-11.2.3 Real Call Edge Extraction
