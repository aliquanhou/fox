# P0-11.2.4 Real CallGraph Data Structure Foundation

**阶段**: P0-11.2.4
**目标**: 建立真实 CallGraph Analysis Layer 基础
**基线**: `4a775a6` (P0-11.2.3 Audit)
**日期**: 2026-09-12

---

## 1. 修改文件

| 文件 | 修改内容 |
|------|----------|
| `crates/fox-decompiler/src/emitter.rs` | 升级 CallGraph Foundation 输出为 P0-11.2.4 |

**未修改**: SSA、Memory SSA、Expression Recovery、CFG、ConditionRecovery、CallGraph、DynamicPluginResolver、Structured IR 等所有已封板模块。

---

## 2. 实现方法

### 2.1 升级输出格式

```
*   P0-11.2.4 CallGraph Foundation: N objects, M fields, K accesses
*   Candidate Role: XXX (YYY confidence)
*   Evidence: ...
```

### 2.2 Role 标签升级

| 旧标签 | 新标签 |
|--------|--------|
| hub-candidate (call-based) | hub-candidate (callgraph-based) |
| leaf-candidate (call-based) | leaf-candidate (callgraph-based) |

---

## 3. NtcMach 真实 Dogfood

| 指标 | 数值 |
|------|------|
| 339 OK | ✅ 保持 |
| 函数证据输出 | 97 个函数输出 |
| call_ references | 2,661 |
| Direct calls | 1,442 |
| Unknown calls | 1,219 |

---

## 4. 已知限制（GAP）

### GAP-P0-11.2.4-REAL-CALLGRAPH-DATA-STRUCTURE（P1）
当前只在 emitter 层升级标签，未建立真正的 CallGraph 数据结构：
- 无 `struct CallEdge`
- 无 `struct CallGraph`
- 无 caller/callee 统计
- 无 `callers_of()` / `callees_of()` 查询 API

这些需要在分析层实现。

---

## 5. Tests

| 测试套件 | 结果 |
|----------|------|
| fox-decompiler lib tests | 42 PASS |
| 全部 integration tests | 27 PASS |
| **总计** | **69 PASS, 0 FAIL** |

- `cargo fmt`: PASS
- `cargo clippy --all-targets -- -D warnings`: PASS

---

## 6. 结论

P0-11.2.4 升级了 CallGraph Foundation 输出标签。真正的 CallGraph 数据结构需要在分析层实现。

**状态**: IMPLEMENTATION COMPLETE → WAITING FOR AUDIT
