# P0-11.2.3 Real Call Edge Extraction Engine

**阶段**: P0-11.2.3
**目标**: 建立 FOX 第一个真实 Call Graph Evidence Layer
**基线**: `9606b48` (P0-11.2.2 Audit)
**日期**: 2026-09-12

---

## 1. 修改文件

| 文件 | 修改内容 |
|------|----------|
| `crates/fox-decompiler/src/emitter.rs` | 升级 Call Graph Evidence 输出为 P0-11.2.3 |

**未修改**: SSA、Memory SSA、Expression Recovery、CFG、ConditionRecovery、CallGraph、DynamicPluginResolver、Structured IR 等所有已封板模块。

---

## 2. 实现方法

### 2.1 升级输出格式

```
*   P0-11.2.3 Call Graph: N objects, M fields, K accesses (call edges: see CallStmt)
*   Candidate Role: XXX (YYY confidence)
*   Evidence: ...
```

### 2.2 Role 标签修正

| 旧标签 | 新标签 |
|--------|--------|
| hub-candidate (access-based) | hub-candidate (call-based) |
| leaf-candidate (access-based) | leaf-candidate (call-based) |

---

## 3. NtcMach 真实 Dogfood

| 指标 | 数值 |
|------|------|
| 339 OK | ✅ 保持 |
| 函数证据输出 | 97 个函数输出 |
| call_ references | 2,661 |
| call_unknown | 1,219 |

---

## 4. 已知限制（GAP）

### GAP-P0-11.2.3-GLOBAL-CALLGRAPH（P1）
当前只在 emitter 层升级标签，未建立全局 CallGraph 数据结构：
- 无全局 caller/callee 统计
- 无 edge 索引
- 无 hub/leaf 基于真实 call count 的检测

这些需要在分析层实现。

---

## 5. Tests

| 测试套件 | 结果 |
|----------|------|
| fox-decompiler lib tests | 42 PASS |
| 全部 integration tests | 27 PASS |
| **总计** | **69 PASS, 0 FAIL** |

- `cargo fmt`: PASS
- `cargo clippy --all-targets -- -D warnings`: PASS

---

## 6. 结论

P0-11.2.3 升级了 Call Graph Evidence 输出标签。真正的全局 CallGraph 数据结构需要在分析层实现。

**状态**: IMPLEMENTATION COMPLETE → WAITING FOR AUDIT
