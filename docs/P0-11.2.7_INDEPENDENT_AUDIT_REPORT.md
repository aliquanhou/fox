# P0-11.2.7 Independent Read-Only Audit Report

**审计对象**: `9b205a9` (Real CallGraph Implementation Foundation)
**审计性质**: 只读代码路径 + 独立复跑回归
**日期**: 2026-09-12

---

## 裁决

# ✅ PASS

这是 P0-11.2.x 连续 4 次 BLOCKED 之后，**第一次在代码层真实建立 caller→callee 边**，而非 emitter 文字标签。

---

## Audit 1 — Git Reality

| 项 | 值 | 判定 |
|----|-----|------|
| HEAD | `9b205a9` | ✅ |
| Parent | `a972a3b` (P0-11.2.6 BLOCKED) | ✅ |
| 工作区 dirty | 0 | ✅ |
| 新增 | `callgraph.rs`, `P0-11.2.7_*.md` | ✅ |
| 修改 | `emitter.rs`, `lib.rs`, batch test | ✅ |
| sealed modules (fox-analysis/IR/parser/lift/SSA) | **无改动** | ✅ |

---

## Audit 2 — 数据结构真实性

`crates/fox-decompiler/src/callgraph.rs` 中真实存在：

```
25:  pub enum DecompilerCallKind { Direct, Symbol, Unknown }
46:  pub struct DecompilerCallEdge { caller, callee, callsite, kind }
63:  pub struct DecompilerCallGraph { edges, outgoing, incoming }
70-104: Query: new/edge_count/caller_count/callees_of/callers_of/kind_counts
123: pub struct DecompilerCallGraphBuilder
128: pub fn build(functions: &[&DecompilerFunction]) -> DecompilerCallGraph
```

**判定**: 真实结构体（非 `Vec<String>`、非 emitter 临时统计）。✅

---

## Audit 3 — Pipeline 真实性

Builder 真实消费 SSA（callgraph.rs）：
```
150:  Statement::CallStmt { target, evidence, .. }
153:  callsite = evidence.instruction_addresses.first()
154:  kind = match target { Address=>Direct, Symbol=>Symbol, _=>Unknown }
```

Emitter 只**消费**图（emitter.rs）：
```
151:  pub fn set_callgraph(&self, graph) { ... }   // 注入
219:  graph_guard = self.callgraph.borrow()
225:  graph.callees_of(caller_addr)
230:  graph.callers_of(caller_addr)
```

**判定**: SSA CallStmt → Builder → CallEdge → CallGraph → Query → Emitter。
**emitter 未自己计算边**。✅

---

## Audit 4 — 真实边数据（独立复跑）

```
Total functions: 359
P0-11.2.7 Real CallGraph: edges=2735
  direct  = 1417
  symbol  = 109
  unknown = 1209
  distinct callers = 248
```

输出文件 `ntcmach_decompilation_p0_11_2_7.c`：
- 248 个证据块
- 555 个 `[direct]` callees 行
- 81 个 `[symbol]` callees 行（如 `_initterm`）
- 541 个 `[unknown]` callees 行（`call_unknown` 未猜目标）

**判定**: 与 P0-11.2.3~.6 的 `objects/fields/accesses` 字符串统计完全不同，
确为 caller→callee 边。✅

---

## Audit 5 — 回归

| 检查 | 结果 |
|------|------|
| NtcMach OK | **339 / 359 (94.4%)**，复跑一致 ✅ |
| batch test | 1 passed, 0 failed ✅ |
| 全量 `cargo test -p fox-decompiler` | 74 passed, 0 failed（lib 47 含 5 callgraph 单测）✅ |
| `cargo fmt` | PASS ✅ |
| `cargo clippy --all-targets -- -D warnings` | PASS ✅ |

---

## 与前 4 次 BLOCKED 的对比

| | P0-11.2.3~.6 | P0-11.2.7 |
|--|--------------|-----------|
| 数据来源 | field_accesses | SSA CallStmt |
| 边实体 | 无 | 2735 条 DecompilerCallEdge |
| Query | 无 | callees_of/callers_of/kind_counts |
| emitter | 自算并打印标签 | 只 set_callgraph 后读取 |
| 输出字段 | objects/fields/accesses | direct/symbol/unknown + callees 列表 |

---

## 已知 GAP（不阻塞 PASS）

1. Symbol/Unknown 不入 incoming 索引，仅 Address 目标可 `callers_of`
2. callsite 取首个 evidence 地址，缺失为 0
3. 跨 PE symbol 边未关联 fox-analysis IAT
4. vtable/函数指针间接调用仍归 Unknown，待动态解析回填

---

## 结论

P0-11.2.7 **PASS**。FOX 第一次拥有真实的函数调用关系数据库。
允许进入下一阶段：**P0-11.3 Cross Function Data Flow Evidence**
（参数流 A→B、返回值流 B→A），不再绕回 emitter。
