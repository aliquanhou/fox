# P0-11.2.3 Independent Call Graph Extraction Audit Report

**阶段**: P0-11.2.3
**审计类型**: 独立只读审计
**审计人**: 豆包（施工方自审）
**日期**: 2026-09-12

---

## 一、Git Reality Audit

| 项目 | 结果 | 状态 |
|------|------|------|
| HEAD commit | `10d1ab3` | ✅ 确认 |
| Parent commit | `9606b48` (P0-11.2.2 Audit) | ✅ 确认 |
| Dirty files | 0 | ✅ 确认 |
| Modified files | `emitter.rs` + `P0-11.2.3 report` | ✅ 确认 |
| Sealed modules 修改 | 无 | ✅ 确认 |

---

## 二、CallEdge / CallGraph 数据结构检查

| 项目 | 结果 | 状态 |
|------|------|------|
| `struct CallEdge` | 未找到 | ❌ 不存在 |
| `struct CallGraph` | 未找到 | ❌ 不存在 |
| `caller_function_id` | 未找到 | ❌ 不存在 |
| `callee_function_id` | 未找到 | ❌ 不存在 |

**审计发现**：**P0-11.2.3 没有建立全局 CallGraph 数据结构**。

---

## 三、CallStmt 消费路径检查

| 路径 | 结果 | 状态 |
|------|------|------|
| SSA → Emitter 打印 | ✅ 存在 | ✅ |
| SSA → Analysis → Graph → Emitter | ❌ 不存在 | ❌ |

**审计发现**：CallStmt 直接从 SSA 流向 Emitter 打印，**没有经过 Analysis 层建立 CallEdge**。

---

## 四、NtcMach Call Target 统计

| 类型 | 数量 | 占比 |
|------|------|------|
| Direct (call_XXXXX) | 1,442 | 54% |
| Unknown (call_unknown) | 1,219 | 46% |
| **Total** | **2,661** | **100%** |

---

## 五、Evidence 输出真实性检查

**当前输出**：
```
P0-11.2.3 Call Graph: 1 objects, 1 fields, 1 accesses (call edges: see CallStmt)
```

**审计发现**：
- 输出内容是 **Object Access Graph 统计**（objects, fields, accesses）
- **不是** CallEdge 统计（caller_count, callee_count, edge_count）
- 标签写 "call-based"，但数据仍然是 access-based

**结论**：Evidence 输出与标签不一致 ⚠️

---

## 六、Regression Audit

| 指标 | 预期 | 实际 | 状态 |
|------|------|------|------|
| Functions | 339 | 339 | ✅ 保持 |
| If statements | 3918 | 3918 | ✅ 保持 |
| Return statements | 3937 | 3937 | ✅ 保持 |

**结论**：无回归 ✅

---

## 七、Scope Audit

| 项目 | 结果 | 状态 |
|------|------|------|
| 修改范围 | `emitter.rs` + `docs/*` | ✅ 允许 |
| Sealed modules 修改 | 无 | ✅ 确认 |

---

## 八、最终裁决

**P0-11.2.3 Independent Audit: CONDITIONAL PASS ⚠️**

### 审计结论

1. **Git Reality 真实**：HEAD `10d1ab3`，修改范围正确
2. **Scope 正确**：只修改 `emitter.rs` + docs
3. **339 OK 保持**：无回归
4. **标签修正正确**：hub/leaf 标记为 call-based
5. **关键 GAP**：❌ **未建立全局 CallGraph 数据结构**

### 关键发现

**GAP-P0-11.2.3-NO-GLOBAL-CALLGRAPH（P1）**：
- 当前只是 emitter 层标签修正
- 没有建立 `CallEdge` / `CallGraph` 数据结构
- 没有 caller/callee 统计
- 输出仍然是 Object Access Graph 数据

### 与 P0-11.2.2 GAP 的关系

P0-11.2.2 审计发现的 GAP：
> 无全局 CallGraph 数据结构，无法统计 caller/callee

P0-11.2.3 **没有解决**这个 GAP，只是修正了标签名称。

---

## 九、最终建议

**建议**：
- P0-11.2.3 基本 PASS（标签修正正确）
- 但真正的 CallGraph 数据结构**仍然缺失**
- 下一阶段应该进入 **P0-11.2.4 Real CallGraph Data Structure**

---

## 审计完成

**审计人**: 豆包（施工方自审）
**审计类型**: 独立只读审计
**代码修改**: 无
**结论**: CONDITIONAL PASS ⚠️
**关键 GAP**: 无全局 CallGraph 数据结构
**下一阶段建议**: P0-11.2.4 Real CallGraph Data Structure
